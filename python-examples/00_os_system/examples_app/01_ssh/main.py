#!/usr/bin/env python3

"""
SSH Connection Example using Paramiko
Step 1) Install paramiko if not already installed:
pip install paramiko
Step 2) Install dotenv if not already installed:
pip install python-dotenv
Step 3) Create a .env file in the same directory with the following content:
    SSH_HOST=your_ssh_server_ip_or_hostname
    SSH_PORT=22
    SSH_USER=your_ssh_username
    SSH_PASSWORD=your_ssh_password
"""
